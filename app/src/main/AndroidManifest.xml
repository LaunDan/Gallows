<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gallows.p">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity" android:label="@string/app_name">

        </activity>
        <activity android:name=".MainGameActivity" android:label="@string/app_name">

        </activity>
        <activity android:name=".EndGameActivity" android:label="@string/app_name">

        </activity>
        <activity android:name=".ShowScoreActivity" android:label="@string/app_name">

        </activity>
        <activity android:name=".TopicActivity" android:label="@string/app_name">

        </activity>
        <activity android:name=".WelcomeActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>